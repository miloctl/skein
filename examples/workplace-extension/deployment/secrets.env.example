# Used only when ATLAS_API_URL is set in the deployment overlay.
ATLAS_API_TOKEN=replace-with-a-secret-reference

# Goes in the skein-db-secret Secret, not atlas-skein-secrets. The
# application role, never the bootstrap superuser.
SKEIN_DATABASE_URL=postgresql://skein:replace-me@skein-db:5432/skein
